# Olaytech 6.1 Homepage Layout + Unified Navigation

This package updates the homepage to match the confirmed mockup direction and locks the same navigation/header style across uploaded pages.

Upload and overwrite all files in this package. If GitHub says too many files, upload in batches:
1. HTML files + css + js
2. assets/brand + assets/home + assets/factory
3. assets/products

The contact form Web3Forms action is preserved in contact.html. This package uses safe wording such as Factory Audit Support instead of unverified certification claims.
