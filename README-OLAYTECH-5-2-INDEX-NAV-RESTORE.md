# Olaytech 5.2 Index + Navigation + Style Restore

This repair package fixes the broken homepage caused by missing `css/style.css` and missing image assets.

Upload these files/folders to GitHub and commit:

- All `.html` files in this package
- `css/style.css`
- `css/support-center.css`
- `css/application-catalog.css`
- `css/header-stable-v3.css`
- `js/`
- `assets/brand/`
- `assets/factory/`

The contact form still uses Web3Forms and keeps the same access key.
